name: Loan MLOps CI/CD

on:
  push:
    branches: [main]

jobs:
  train:
    runs-on: ubuntu-latest

    steps:
      # ----------------------------
      # Checkout repo
      # ----------------------------
      - name: Checkout repo
        uses: actions/checkout@v4

      # ----------------------------
      # Setup Python
      # ----------------------------
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # ----------------------------
      # Install dependencies
      # ----------------------------
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt
          pip install dvc

      # ----------------------------
      # Debug file structure
      # ----------------------------
      - name: Debug file structure
        run: |
          echo "Current directory:"
          pwd
          echo "Listing files:"
          ls -R

      # ----------------------------
      # Pull data from DVC (optional, safe)
      # ----------------------------
      - name: Pull data from DVC
        run: dvc pull || echo "DVC pull failed, continuing..."

      # ----------------------------
      # Run training with PYTHONPATH fix
      # ----------------------------
      - name: Run training
        run: |
          export PYTHONPATH=$PYTHONPATH:$(pwd)
          python src/train.py

      # ----------------------------
      # Upload artifacts
      # ----------------------------
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: trained-models
          path: |
            models/
            mlruns/

  docker:
    runs-on: ubuntu-latest
    needs: train

    steps:
      # ----------------------------
      # Checkout repo again
      # ----------------------------
      - name: Checkout repo
        uses: actions/checkout@v4

      # ----------------------------
      # Setup Docker Buildx
      # ----------------------------
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ----------------------------
      # Login to Docker Hub
      # ----------------------------
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ----------------------------
      # Verify Docker login (new step)
      # ----------------------------
      - name: Verify Docker login
        run: docker info

      # ----------------------------
      # Build & Push Docker Image
      # ----------------------------
      - name: Build & Push Docker Image
        run: |
          docker build -t loann-mlops:latest .
          docker tag loann-mlops:latest zainakhann/loann-mlops:latest
          docker push zainakhann/loann-mlops:latest
          
          
          
